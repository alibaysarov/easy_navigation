repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.3  # можно обновлять
    hooks:
      - id: ruff-format
        files: ^backend/
        stages: [pre-commit]
  - repo: local
    hooks:
      - id: pytest-backend
        name: pytest backend
        entry: bash -c "cd backend && pytest tests/"
        language: system
        pass_filenames: false
        stages: [pre-push]